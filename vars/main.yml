---
# users needed to run docker
#docker_extra_users:
  #- user1
  #- user2

# let group to access to docker "data" path and default volumes path
# docker_data_path: /var/lib/docker/data
# granded_group_to_data: docker
# granded_read_paths:
#   - /var/lib/docker/volumes
# granded_write_paths:
#   - /var/lib/docker/data

#Installed alias in /usr/share (only with root user of become)
#bash_alias_dir_share: /usr/share
#bash_alias_shared: yes

#docker_networks:
# - name: proxy-net
#   subnet: 10.10.11.0/24
#   gateway: 10.10.11.1